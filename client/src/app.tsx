import * as React from "react";
import { BrowserRouter } from "react-router-dom";

const App: React.FC = () => (
  <BrowserRouter>
    <div>{"Hello, World!"}</div>
  </BrowserRouter>
);

export default App;
